CREATE DATABASE IF NOT EXISTS college_erp;
USE college_erp;

-- The Flask application creates the tables automatically with SQLAlchemy.
-- This file is included so you can create the database manually if preferred.
